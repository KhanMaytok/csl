if ('<%= @pay.errors.any? %>' == 'true') {
	$('#to_update_principal').html("<%= j (render 'to_update_principal' )%>");
	var errors = ("<%=  @pay.errors[:code_repeated] %>");
	alert(errors);

}
else{
	$('#to_update_principal').html("<%= j (render 'to_update_principal' )%>");
	alert('Cambios completos <%= @pay.errors.any? %>');
}