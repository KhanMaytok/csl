<h1>Agregar compañía</h1>
<div class="row">
	<div class="col-md-6">
		<%= form_tag(create_company_path, method: 'post', role: 'form') do %>
			<div class="form-group">
				<div class="row">
					<div class="col-md-7">
						<%= link_to '¿No se encuentra la compañía? Regístrala.', '#' %>
						
					</div>				
				</div>
				
			</div>
			<div class="form-group">
				<%= text_field_tag :ruc, '', {placeholder: 'Ruc de la empresa', class: 'form-control'} %>
			</div>
			
			<div class="form-group">
				<%= label_tag :name, 'Nombre de la empresa' %>
				<%= text_field_tag :name, '', {placeholder: 'Nombre de la empresa', class: 'form-control'} %>
			</div>
			<div class="form-group"></div>

			
			<div class="form-group">
				<%= submit_tag 'Registrar', class: 'btn btn-primary' %>
			</div>
		<% end %>
	</div>
	<div class="col-md-6">
		<h3>¿Está usted seguro que no existe?</h3>
		<p>Puede verificarlo en la lista de aquí abajo. Recuerde que duplicar un registro pude traer graves consecuencias al sistema.</p>
		<%= select_tag :asd, options_for_select(@companies),  {size: 20, multiple: true, class: 'form-control'} %>
	</div>
</div>