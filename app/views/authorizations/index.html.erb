<h1>Autorizaciones</h1>
<center>Actualmente se han ingresado <%= @authorizations.count %> autorizaciones con el sistema SITEDS</center><br>
<div class="row">
	<div class="col-md-5">
		<%= form_tag(authorizations_path(page: 1), method: 'get', role: 'form') do %>
			<div class="form-group">
				<%= text_field_tag :authorization_code, '',  {class: 'form-control', placeholder: 'Código de authorización'} %>
			</div>			
		<% end %>
		<div class="form-group">
			<%= form_tag(authorizations_path(page: 1), method: 'get', role: 'form') do %>
				<%= text_field_tag :paternal, '', {class: 'form-control', placeholder: 'Apellido paterno'} %>
		</div>
		<% end %>
	</div>
	<div class=""></div>
</div>
<table class="table  table-striped">
	<thead>
		<tr>
			<td>#</td>
			<td>Nombre de Paciente</td>
			<td>Nombre de Aseguradora</td>
			<td>Link hacia autorización</td>
		</tr>
	</thead>
	<tbody>
	<% @authorizations.each do |a| %>
		<tr>
			<td><%= a.id %></td>
			<td><%= link_to to_name_i(a.patient), show_insured_path(id: a.patient.id) %></td>
			<td><%= a.patient.insured.insurance.name %></td>	
			<% if a.code != nil %>
			<td><%= link_to 'ir a '+ a.code, show_authorization_path(id: a.id) %></td>
			<% end %>
		</tr>
	<% end %>
	</tbody>
</table>
<br>
<center><%= will_paginate @insureds, renderer: BootstrapPagination::Rails %></center>

