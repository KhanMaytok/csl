<h1>Registro de Servicios de Tópico : 	</h1>
<p>¿Confirma que desea ingresar una nueva venta?</p>
<p>
	<%= form_tag(confirm_topic_path, method: 'post') do %>
		<%= hidden_field_tag :id_authorization, params[:id_authorization] %>
		<%= hidden_field_tag :name, params[:name] %>		
		<%= label_tag :doctor_id , 'Doctor encargado' %>
		<%= select_tag :doctor_id, options_for_select(@doctors) %>
		<%=submit_tag 'Confirmar', data: {confirm: '¿Está seguro que desea empezar una compra?'} %>
	<% end %>
</p>